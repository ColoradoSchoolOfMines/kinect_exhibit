package edu.mines.csci598.recycler.frontend;

import org.apache.log4j.Logger;

/**
 * An enum to represent the side of the game.
 */
public enum Side {
	LEFT, RIGHT;	
	

    private static final Logger logger = Logger.getLogger(Side.class);
}
